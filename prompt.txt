Hey analyse and understand the current repository structure completely first . Make a very deep dive into the current repository stricture fully. I want some upgrades and improvements for my web pannel.
Upgrade 1 : 
From now onwards what every session are uploaded through the frontend and are once login they must remained login until they are manually logout from the pannel. The session must remain active and must remain login into the pannel until the session is manually logout through the front end. strictly it should remain active . If multiple session are logged in than they mist remain active until the docker container of are running or until the pannel is up. Strictly make sure everythinf works fine and remain active.
Upgrade 2 :
Nextly i want to add a new upgrade or new option to the frontend and complete working for that feature on the backend. The upgrade is that make a new option or page on the front end through which i can change the 2fa password of the sessions that are logged in . On the front end Create a Change 2fa option and on tapping that two options must appear. First one will be change password for multiple sessions at once ( for that the old and new password must be same for all ). in the top field the user need to enter the old password in the bewlo fueld new password ( the password to change ) than needs to select all active session from the list for which password needs to be change than hit confirm ( than a job must be created for this task and there must be history section below in which status kf job must be visible ). if for some the old password doesn't worked than dont stop the whole job skil that session move to next like that and after job is completed provide the results ( success , failed ). You need to built a complete solid bullet proof backend working for that . The number of sessions can be even 100 or more . Nextly in the second option there should be option to change 2fa pass for sessions one by one. like bot will list all the active session ( even search for session option should be there like by session name the session must be visible ) than when user tap on a session for that particular session two options mist came old pass and new pass ( tied to that session only ) like this than once for this session si dont user can select other session and similar options must came for that and like that it continues at last user hit confirm and the job is started again. You have to build a very bullet proof institutional level system at the backend. for this to work with full smooth front end ( animated and ui according to the current theme ). Like this the working should be .
Upgrade 3 : 
Nextly i want you to add a new option on the frontend and complete working for that on the backend. The new option will be of get otp . in that option on the fronted all active session must be listed and user can select the sessions for which they want to get the login code . once the number of sessions is selected than when user hits confirm than bot should start a job for next 5 minutes and should notify the user that bot will has started scanning for the otp in all the sessions for the next 5 minutes. Please request a code . on the frontend an active job should be started for the next 5 minutes that will start scanning the login otp for all the session once the otp is detected on a session the instantly in the history next to that session otp must ne present for that session ( without actually refereshing the site ). the otp update must ne dynamic . same for all other sessions also. design a very solid backend for this and dynamich upgdates on the frontend.
Upgrade 4 :
Nextlt i want you to add next level proxy system in the backend and option for that on the front end. By default built such a system in the backend the ultimate proxy system . As the pannel is designed to handle multiple session at once but here's the catch telegrsm will not allow requesting multiple login codes for multipler session through same ip address. So you need to built such a dynamic proxy system or proxy scrapper in the backend that will actively scans for available free open source proxies ( that are actually working and are not detected by telegram system ) once the scrapoer or whatever you designed in the backend finds an active free opensource proxy ( that is actually working ) it should store that proxy in database like this in the backend upto 20 proxies muste be stired that are actually working and the scrapoer again test them for in an 10 minutes if they found to be dead remove them and tried to find new again. it will be default working.But also gives a proxy section on the front end thriugh which user can add manual ( paid proxy) and if a manual or paid proxy is added and is detected as working than that proxy must be given top priority amog all free proxies.
Now here is how the working should be if proxies are available ( free or paid ) than bot must only login 4 accounts from one ip ( firstly from the vps ip through which the pannel is running than next 4 from other next 4 from other like that ). Design everything full bullet proof on the backend.

Tests :
I have added an active session in the session directory of the repository. test every feature with that session by logging it. run the setup and complete pannel on your terminal than create a api eky for web login and test everything until everything is foudn wlrking without any errors. complete everything in one mega pr . you can do multiple commits if you want
